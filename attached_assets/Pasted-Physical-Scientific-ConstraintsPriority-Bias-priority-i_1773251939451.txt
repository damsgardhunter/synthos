Physical & Scientific ConstraintsPriority Bias: priority is derived as Math.round((candidate.ensembleScore ?? 0) * 100) (line 376).Scientific Note: In the Quantum Alchemy Engine, the ensembleScore often prioritizes high $T_c$. This means the DFT queue will always be filled with high-$T_c$ candidates (Exploitation).Improvement: The refill logic should use the acquisitionScore (which includes uncertainty) to ensure the DFT workers also process "weird" materials to improve the global model accuracy (Exploration).Hardcoded Default Lattices: If the prototypeUsed metadata is missing, the worker likely falls back to a simple cubic lattice (as seen in qe-dft-engine.ts).Constraint: Without a structure from the Structure Predictor, the DFT results ($E_{total}$ and $DOS$) are physically meaningless.Requirement: Ensure refillQueueIfLow only selects candidates that already have a successful structure_prediction record.3. Performance & Structural ImprovementsParallel Job Launching: You use Promise.all(launched) to process up to MAX_CONCURRENT jobs (line 424).Performance: This is excellent for high-throughput. However, because processNextJob involves spawning heavy external pw.x processes, a burst of 4 concurrent starts can cause a CPU spike that makes the server unresponsive.Improvement: Use a staggered start (e.g., await delay(2000)) between job launches within the slotsAvailable loop.Metrics Serialization overhead: getStageMetrics uses JSON.parse(JSON.stringify(stageMetrics)) (line 462).Optimization: While this creates a deep clone to prevent external mutation, it is expensive if called frequently by the frontend. Since stageMetrics is a flat object, a simple spread {...stageMetrics} is sufficient.Stale Job Cleanup Timing: cleanupStaleJobs is only called once at startup (line 408).Improvement: In a production-grade discovery run, jobs can hang due to filesystem locks or MPI errors. Stale cleanup should be part of the 30-second loop() to ensure worker slots aren't permanently occupied by "Zombie" jobs.
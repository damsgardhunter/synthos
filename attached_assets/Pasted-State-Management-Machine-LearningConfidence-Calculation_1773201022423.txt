State Management & Machine LearningConfidence Calculation Complexity:TypeScriptensembleConfidence = gnnResult.confidence * 0.6 + gbResult.score * 0.4;
The Logic Trap: gbResult.score is usually the output of a classifier (0 to 1). gnnResult.confidence is a separate metric.Fix: Ensure these are calibrated. If the GNN is highly confident but predicts a $T_c$ of 0, while the GB is weakly confident but predicts 200K, the weighted average might result in a "Medium Confidence 80K" result which is physically baseless. Add a "Disagreement Penalty": if models diverge by more than 50K, slash the ensembleScore.The "Double Update" Database Problem:Inside runAutonomousDiscoveryCycle, you call insertCandidateWithStabilityCheck, which already contains a stability check. Then, later in the same function, you perform runXTBPhononCheck.The Constraint: This results in redundant computation.Fix: Structure the Discovery Cycle so that the "Guardrail" checks (Chemistry, Pre-filter) happen once, and then the candidate is passed to a single "Physical Verification" function that handles both xTB and database insertion.4. Technical Logic & SafetyInsert Success Logic:TypeScriptif (isDuplicate) { autonomousInserted = true; } else { autonomousInserted = false; }
The Bug: If the insert fails because of a duplicate, you mark it as "True" (Success) and then proceed to totalScCandidates++.The Impact: This will double-count your statistics. Your totalScCandidates counter will increment even though no new material was added to the database.Fix: Only increment totalScCandidates and recentNewCandidates if the insertion was not a duplicate."Below Tier" Rejection Log:TypeScriptif (tier === 0) { return { passed: false, tc: finalTc, reason: "below-tier3-thresholds" }; }
Improvement: You are currently discarding these. Even if a material isn't a "Tier 1" candidate, its physics data ($\lambda$, $\omega_{log}$) is valuable training data for the next Active Learning retrain.Fix: Store "Negative" results in a separate FailedCandidates table to prevent the engine from re-calculating the same mediocre materials in the next cycle.
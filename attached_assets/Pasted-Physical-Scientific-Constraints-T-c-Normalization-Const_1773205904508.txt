Physical & Scientific Constraints$T_c$ Normalization Constant: You use Math.min(1, s.bestTc / 200).Scientific Note: While 200K is a high bar, superhydrides (which you target in family-filters.ts) have already reached 250K–287K. If a generator finds a 280K material, its score saturates at 1.0, the same as a 200K material.Improvement: Increase the normalization ceiling to 300K to provide a "gradient" for room-temperature discovery.Novelty Score Decay: You use an Alpha of 0.1 for a moving average: noveltyScore * (1 - alpha) + novelty * alpha.Constraint: This means the novelty of a generator is heavily biased toward its most recent batch. In materials discovery, a generator might produce 100 variants of a known material (low novelty) to find one outlier. The current decay might "punish" a generator too quickly for a single standard batch.3. Numerical Stability & PerformanceSoftmax Temperature Sensitivity: Your temperature = 2.0 in rebalanceWeights is a good "middle ground" (not too greedy, not too random).Refinement: As the rebalanceCount increases (indicating a maturing discovery campaign), you should decrease the temperature (annealing). This allows the engine to start with broad exploration and gradually focus "all hands" on the winning strategies.Activity Check for Cold Starts: if (s.candidatesGenerated === 0) returns a yield score of 0.The Problem: This creates a "Starvation" trap. If a new generator is registered via registerGenerator, it has 0 activity, so it gets a 0 score, which (after rebalancing) keeps it at the MINIMUM_WEIGHT_FLOOR, where it is less likely to be picked.Fix: Give new generators a "Grace Period" or a default median yieldScore for the first $N$ cycles to allow them to collect enough data for a fair evaluation.4. Structural RecommendationsDFT Weighting vs. Pipeline Weighting:TypeScriptconst pipelineScore = passRate * 0.3 + tcNorm * 0.3 + noveltyNorm * 0.1;
const dftScore = hasDFTData ? (dftDiscovery * 0.2 + dftTcNorm * 0.1) : 0;
Improvement: DFT results are "Ground Truth," whereas pipeline passes are just "Surrogate Model Agreements." You should significantly increase the weight of dftDiscovery relative to passRate. A generator that passes the GNN 90% of the time but fails DFT every time is "hallucinating" and should be penalized more than the current weights allow.Rebalance Interval vs. Tempo: REBALANCE_INTERVAL = 3 is fixed.Improvement: Tie this to the Engine Tempo. In "Excited" tempo, you should rebalance more frequently to catch winning streaks. In "Contemplating" tempo, rebalance less often to avoid reacting to noise.
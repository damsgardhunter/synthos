Critical Numerical & Logic BugsIntegration Bias in lambda Calculation: In parseAlpha2FOutput (lines 230–234), you calculate the coupling constant $\lambda$ using a Riemann sum: lambda += 2 * alpha2F[i] / frequencies[i] * binWidth.The Bug: The denominator frequencies[i] causes the integral to diverge (approach infinity) if there are very low-frequency modes or numerical noise near the $\Gamma$-point ($\omega \approx 0$).Fix: Implement a low-frequency cutoff (e.g., if (frequencies[i] < 1.0) continue;) or use the midpoint of the bin to prevent the "zero-frequency explosion" that would lead to unphysically high $T_c$ predictions.Log-Sum Singularity in omegaLog: In the $\omega_{log}$ calculation (lines 240–243), you use Math.log(frequencies[i]).The Bug: If frequencies[i] is zero or negative (imaginary modes), Math.log will return -Infinity or NaN. While your loop checks frequencies[i] > 0, numerical noise just above zero (e.g., $10^{-9}$) will result in a massive negative logSum, causing omegaLog to vanish toward zero.Fix: Use a regulated log: Math.log(Math.max(frequencies[i], 1e-3)).Constant binWidth Assumption: The code assumes a uniform grid: binWidth = frequencies[1] - frequencies[0].Risk: If the Quantum ESPRESSO lambda.x output has a non-uniform grid or missing points, the entire integral will be scaled incorrectly.Improvement: Calculate a dynamic deltaOmega for each bin based on (frequencies[i+1] - frequencies[i-1]) / 2.2. Physical & Scientific ConstraintsAlpha2F Non-Negativity: You correctly use alpha2F.push(Math.max(0, a2f)) (line 224).Scientific Context: This is a vital physical guardrail. $\alpha^2F(\omega)$ is a spectral weight and cannot be negative. Negative values in DFT outputs are purely numerical artifacts from poor $k$-point or $q$-point sampling."Reconstructed" vs "lambda.x" Source: Your source logic (line 251) differentiates between raw data and derived math.Improvement: You should also check for the hasImaginaryModes flag from the phonon-calculator.ts. If a material has imaginary modes, the $\lambda$ and $\omega_{log}$ values are physically meaningless because the structure is unstable.